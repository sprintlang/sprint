import 0x0.LibraAccount;
import 0x0.LibraCoin;
import {{author}}.{{module}};

main() {
    // TODO: Replace 1 and 0 with parameter
    {{module}}.deposit({{owner}}, LibraAccount.withdraw_from_sender(1), 0);

    return;
}
