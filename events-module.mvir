module M {
    import 0x0.LibraAccount;

    struct Event {}

    public emit_event(handle: &mut LibraAccount.EventHandle<Self.Event>) {
        LibraAccount.emit_event<Self.Event>(move(handle), Event{});
        return;
    }
}
